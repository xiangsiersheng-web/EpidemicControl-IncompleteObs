# Checkpoints included in existing public repository

No checkpoint is duplicated into this release candidate. Existing public checkpoint files must retain their original filename and SHA256; model mapping is in `configs/paper_model_mapping.json`. Before a release tag, add the repository license and third-party data attribution/license information.
